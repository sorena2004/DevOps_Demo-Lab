apiVersion: v1
kind: Service

metadata:
  name: kube-state-metrics
  namespace: monitoring

spec:
  selector:
    app: kube-state-metrics

  ports:
    - name: http
      port: 8080
      targetPort: http

    - name: telemetry
      port: 8081
      targetPort: telemetry

  type: ClusterIP
